# Phase 1 archive (Echo of Roots)

These modules powered the previous platform UI (mentor mode, dialect book, anti-fraud, multi-dialect switcher).
They are unused by the current Nana Agent (`App.jsx`) but kept for reference.

Also archived: former frontend `src/data/*.json` (duplicates of backend knowledge base).

Current Nana Agent active libs:
- `src/lib/voiceEchoMapping.js`
- `src/lib/operaStations.js`

Backend still uses `backend/data/teochew_*.json` for LIVE chat/KB endpoints.
