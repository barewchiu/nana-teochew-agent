fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
httpx[socks]>=0.27.0
socksio>=1.0.0

# Optional — only for TEOCHEW_ASR_MODE=transformers (GPU recommended)
# torch
# transformers>=4.40.0
# accelerate
# librosa
# soundfile
