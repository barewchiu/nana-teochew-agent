# Audio assets

- `opera_*.mp3` — short previews for deploy (GitHub size limits).
- Full-length masters (if any) live in `../audio_full_local/` (gitignored).
- Grandson messages: `back_home.m4a`, `miss_you.m4a`, `remind_meds.m4a`.
